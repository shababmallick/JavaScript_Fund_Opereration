let p = true;
let q = false;
let andResult = p && q //logical AND
let orResult = p || q;
let notResult =!p;

console.log(parseInt(andResult)); 
console.log(andResult);  // Outputs: false
console.log(orResult);   // Outputs: true
console.log(notResult);  // Outputs: false