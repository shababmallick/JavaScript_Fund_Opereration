/* unary operators are operators that work with 
a single operand. 

They perform operations on a single value or variable.

common unary operators include the inctement('++') 
and decrement('--') operators, as well as the
 logical NOT('!') operator

*/

let x = 5;
x++;

console.log(x);